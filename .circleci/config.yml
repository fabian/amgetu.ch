---
version: 2
jobs:
  build:
    docker:
      - image: cibuilds/hugo
    branches:
      only:
        - master
    steps:
      - checkout
      - add_ssh_keys
      - run: hugo
      - deploy:
          command: rsync -avz --delete public/ debian@cloud-vogler-07.nine.ch:/var/www/amgetu/2019
